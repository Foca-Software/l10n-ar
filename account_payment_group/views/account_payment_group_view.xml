<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data>

<record id="view_account_payment_group_list" model="ir.ui.view">
    <field name="name">account.payment.group.list</field>
    <field name="model">account.payment.group</field>
    <field name="arch" type="xml">
        <list decoration-info="state=='draft'" decoration-muted="state in ['reconciled', 'cancel']">
            <field name="payment_date"/>
            <field name="name"/>
            <field name="partner_id"/>
            <field name="payment_methods"/>
            <field name="payments_amount" sum="Total" string="Monto"/>
            <field name="state"/>
            <field name="company_id" groups="base.group_multi_company"/>
            <field name="currency_id" invisible="1"/>
            <field name="partner_type" invisible="1"/>
        </list>
    </field>
</record>


<record id="view_account_payment_group_form" model="ir.ui.view">
    <field name="name">Pago Múltiple</field>
    <field name="model">account.payment.group</field>
    <field name="arch" type="xml">
    	<form string="Registrar Pago">
            <field name="document_sequence_id" invisible="1"/>
            <field name="localization" invisible="1"/>
            <field name="account_internal_type" invisible="1"/>
            <field name="id" invisible="1"/>
            <field name="commercial_partner_id" invisible="1"/>
            <field name="partner_type" invisible="1"/>
            <field name="payment_subtype" invisible="1"/>
            <field name="pop_up" invisible="1"/>
            <field name="has_outstanding" invisible="1"/> 
            <header>
                <button name="action_payment_sent" type="object" string="Enviar por Email" 
                        class="oe_highlight" groups="base.group_user" 
                        invisible="sent or state != 'posted'"/>
                <field name="sent" invisible="1"/>
                <button name="action_payment_sent" type="object" string="Enviar por Email" 
                        groups="base.group_user" 
                        invisible="not sent or state != 'posted'"/>
                <button name="payment_print" string="Imprimir" type="object" 
                        class="oe_highlight" groups="base.group_user" 
                        invisible="sent or state != 'posted'"/>
                <button name="payment_print" string="Imprimir" type="object" 
                        groups="base.group_user" 
                        invisible="not sent or state != 'posted'"/>
                <button name="post" class="oe_highlight" string="Validar" type="object" 
                        invisible="(payment_subtype == 'simple' and state != 'draft') or (payment_subtype == 'double_validation' and state != 'confirmed')"/>
                <button name="compute_withholdings" class="oe_highlight" string="Calcular retenciones" type="object" 
                        invisible="state != 'draft' or partner_type != 'supplier'"/>
                <button name="action_draft"
                        invisible="state != 'cancel' and not (state == 'posted' and partner_type == 'customer')"
                        string="Volver a borrador" type="object"
                        confirm="Esto va a desconciliar los pagos asociados y le va a permitir volver a editarlos o eliminarlos (incluidos cheques). ¿Desea continuar?"/>
                <button name="confirm" class="oe_highlight" string="Confirmar" type="object" 
                        groups="account_payment_group.account_confirm_payment" 
                        invisible="payment_subtype != 'double_validation' or state != 'draft'"/>
                <button name="cancel" string="Cancelar" type="object" 
                        invisible="state == 'cancel'"/>
                <field name="state" widget="statusbar" statusbar_visible="posted"/>
            </header>
            
            
           <div class="alert alert-info" role="alert" style="margin-bottom:0px;" invisible="has_outstanding == 'False'">
                Hay <b>créditos pendientes</b> de imputar en la deuda seleccionada, se intentará imputar dichos créditos a otros comprobantes seleccionados pero dicha información <b>no quedará registrada en el pago</b>. Si no desea que se imputen automáticamente puede borrarlos de la lista.
            </div>  
            
            <sheet>
                <div class="oe_button_box" name="button_box">
                    <button class="oe_stat_button" name="button_journal_entries"
                            string="Apuntes Contables" type="object"
                            groups="account.group_account_manager"
                            invisible="not move_line_ids" icon="fa-bars"/>
                    <field name="move_line_ids" invisible="1"/>
                </div>
                <field name="id" invisible="1"/>
                <div class="oe_title">
	            <h1>
                    <field name="display_name" class="oe_inline" invisible="not display_name"/>
                    <label for="next_number" string="- Siguiente Número: " class="oe_inline" invisible="next_number in (0, False)"/>
                    <field name="next_number" class="oe_inline" invisible="next_number in (0, False)"/>
	            </h1>
                </div>
                    <group>
                        <field name="company_id" options="{'no_create': True, 'no_open': True}" groups="base.group_multi_company" invisible="context.get('pop_up')"/>
                        <field name="currency_id" invisible="1"/>
			<field name="partner_id" context="{'default_is_company': True, 'default_supplier': partner_type == 'supplier', 'default_customer': partner_type == 'customer'}" 
				invisible="context.get('pop_up')"
                         readonly="state in ['posted', 'confirmed', 'cancel']" />
                    </group>
                    <group>
                        <field name="receiptbook_id" 
                               invisible="localization == False"
                               required="localization != False and state == 'draft'"
                               domain="[('partner_type', '=', partner_type), ('company_id', '=', company_id)]"
                               readonly="state in ['posted', 'confirmed', 'cancel']"
                               groups="base.group_multi_company" />
                        <field name="document_number" 
                               invisible="document_sequence_id != False or localization == False"
                               required="document_sequence_id == False and localization != False"
                               readonly="state in ['posted', 'confirmed', 'cancel']" />
                        <field name="payment_date"
                               readonly="state in ['posted', 'confirmed', 'cancel']" />
                        <field name="communication" 
                               readonly="state in ['posted', 'confirmed', 'cancel']"
                               invisible="state != 'draft' or communication == False" />
                    </group>
                    <group>
                        <field name="selected_debt" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="state not in ['draft', 'confirmed']"/>
                        <field name="related_invoice" 
                               invisible="related_invoice == False" />
                        <field name="related_invoice_amount" 
                               invisible="related_invoice == False" />
                        <field name="selected_finacial_debt" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="state not in ['draft', 'confirmed']"
                               groups="account_financial_amount.account_use_financial_amounts"/>
                        <field name="matched_amount" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="state != 'posted'"
                               string="Monto a conciliar"/>
                        <field name="unmatched_amount" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="state != 'posted'"
                               string="Monto sin conciliar"/>
                        <field name="unreconciled_amount" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="payment_subtype != 'double_validation' or state not in ['draft', 'confirmed']"/>
                        <field name="to_pay_amount" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="payment_subtype != 'double_validation' or state not in ['draft', 'confirmed']"/>
                    </group>
                    <group>
                        <field name="payments_amount" 
                               string="Monto pago" 
                               options="{'currency_field': 'currency_id'}"/>
                        <field name="payment_difference" 
                               options="{'currency_field': 'currency_id'}" 
                               invisible="state not in ['draft', 'confirmed']"/>
                    </group>
                    <notebook>
                        <page string="Líneas a pagar">
                            <button name="add_all" 
                                    string="Agregar Todos / Refrescar" 
                                    invisible="state != 'draft'"
                                    type="object"/>
                            <button name="remove_all" 
                                    string="Remover Todos" 
                                    invisible="state != 'draft'" 
                                    type="object"/>
                            
                            <field name="to_pay_move_line_ids" 
                                   domain="[('partner_id.commercial_partner_id', '=', commercial_partner_id),('account_id.account_type', '=', account_internal_type),('account_id.reconcile', '=', True),('reconciled', '=', False),('full_reconcile_id', '=', False),('company_id', '=', company_id),('move_id.debo_payment_type', '=', 'checking_account')]" 
                                   groups="base.group_multi_company" />
                        </page>
                        <page string="Lineas de pagos">
                                   <field name="payment_ids"
		                                  context="{
		                                      'list_view_ref': 'account_payment_group.view_account_payment_from_group_list',
		                                      'kanban_view_ref': 'account_payment_group.view_account_payment_kanban_mobile',
		                                      'default_move_journal_types': ('bank', 'cash'),
		                                      'default_company_id': company_id
		                                  }"
			                              readonly="state not in ['draft', 'confirmed']"
					                      groups="base.group_multi_company">
                                            <!-- Formulario propio de esta vista (copia de account.view_account_payment_form
                                                 + las modificaciones de view_account_payment_form/view_account_payment_from_group_form
                                                 de este módulo). Se define inline a propósito para que sea modificable solo
                                                 acá, sin afectar view_account_payment_from_group_form ni las vistas padre.
                                                 OJO: al ser un arch propio, NO hereda automáticamente los campos que agregan
                                                 otros módulos instalados sobre account.view_account_payment_form (retenciones,
                                                 cheques, cash control, recargos financieros, etc.); esos campos no se
                                                 mostrarán en este diálogo aunque sigan existiendo en el resto de la app. -->
                                            <form string="Pay">
                                                <header>
                                                    <button name="action_post" string="Confirm" type="object" class="oe_highlight"
                                                            invisible="state != 'draft'" groups="base.group_no_one" data-hotkey="q"
                                                            confirm="IMPORTANTE: La validación de la línea de pago se debe hacer desde el recibo. La validación manual de una línea de pago se usa solo para correcciones y usos avanzados. Seguro desea continuar?"/>
                                                    <button name="action_validate" string="Validate" type="object" class="oe_highlight"
                                                            invisible="state != 'in_process' or move_id" data-hotkey="q"/>
                                                    <button name="action_reject" string="Reject" type="object" class="oe_highlight"
                                                            invisible="state != 'in_process' or not is_sent" data-hotkey="q"/>
                                                    <button name="action_draft" string="Reset To Draft" type="object" class="btn btn-secondary"
                                                            invisible="state in ('draft')" groups="base.group_no_one" data-hotkey="w"
                                                            confirm="IMPORTANTE: La validación de la línea de pago se debe hacer desde el recibo. La cancelación manual de una línea de pago se usa solo para correcciones y usos avanzados. Seguro desea continuar?"/>
                                                    <button name="action_cancel" string="Cancel" type="object"
                                                            invisible="not id or not (state == 'draft' or (state == 'in_process' and is_sent))" data-hotkey="x"
                                                            groups="base.group_no_one"
                                                            confirm="IMPORTANTE: La validación de la línea de pago se debe hacer desde el recibo. La cancelación manual de una línea de pago se usa solo para correcciones y usos avanzados. Seguro desea continuar?"/>
                                                    <button string="Request Cancel" name="button_request_cancel" type="object"
                                                            groups="account.group_account_invoice"
                                                            invisible="state != 'in_process' or not move_id or not need_cancel_request" data-hotkey="w"/>
                                                    <button name="mark_as_sent" string="Mark as Sent" type="object" data-hotkey="v"
                                                            invisible="state != 'in_process' or is_sent or payment_method_code != 'manual'"/>
                                                    <button name="unmark_as_sent" string="Unmark as Sent" type="object" data-hotkey="k"
                                                            invisible="state != 'in_process' or not is_sent or payment_method_code != 'manual'"/>
                                                    <field name="state" widget="statusbar" statusbar_visible="draft,in_process,paid"/>
                                                </header>
                                                <div class="alert alert-warning mb-0" role="alert" invisible="not duplicate_payment_ids or state!='draft'">
                                                    <span>This payment has the same partner, amount and date as </span>
                                                    <field name="duplicate_payment_ids" widget="x2many_buttons" string="Duplicated Payments"/>
                                                </div>
                                                <sheet>
                                                    <field name="id" invisible="1"/>
                                                    <field name="is_sent" invisible="1"/>
                                                    <field name="need_cancel_request" invisible="1"/>
                                                    <field name="is_reconciled" invisible="1"/>
                                                    <field name="is_matched" invisible="1"/>
                                                    <field name="payment_method_code" invisible="1"/>
                                                    <field name="show_partner_bank_account" invisible="1"/>
                                                    <field name="require_partner_bank_account" invisible="1"/>
                                                    <field name="available_payment_method_line_ids" invisible="1"/>
                                                    <field name="available_partner_bank_ids" invisible="1"/>
                                                    <field name="country_code" invisible="1"/>
                                                    <field name="partner_type" invisible="1"/>
                                                    <field name="reconciled_invoices_type" invisible="1"/>
                                                    <field name="company_id" invisible="1"/>
                                                    <field name="paired_internal_transfer_payment_id" invisible="1"/>
                                                    <field name="available_journal_ids" invisible="1"/>
                                                    <field name="currency_id" invisible="1"/>
                                                    <field name="payment_group_id" invisible="1"/>

                                                    <div class="oe_button_box" name="button_box">
                                                        <button name="button_open_invoices" type="object"
                                                                class="oe_stat_button" icon="fa-bars"
                                                                invisible="reconciled_invoices_count == 0">
                                                            <div class="o_form_field o_stat_info">
                                                                <span class="o_stat_text">
                                                                    <field name="reconciled_invoices_count"/>
                                                                    <span invisible="reconciled_invoices_type != 'invoice'"> Invoice</span>
                                                                    <span invisible="reconciled_invoices_type == 'invoice'"> Credit Note</span>
                                                                </span>
                                                            </div>
                                                        </button>
                                                        <button name="button_open_bills" type="object"
                                                                class="oe_stat_button" icon="fa-bars"
                                                                invisible="reconciled_bills_count == 0">
                                                            <div class="o_form_field o_stat_info">
                                                                <span class="o_stat_text">
                                                                    <field name="reconciled_bills_count"/>
                                                                    <span> Bill</span>
                                                                </span>
                                                            </div>
                                                        </button>
                                                        <button name="button_open_statement_lines" type="object"
                                                                class="oe_stat_button" icon="fa-bars"
                                                                invisible="reconciled_statement_lines_count == 0">
                                                            <div class="o_form_field o_stat_info">
                                                                <field name="reconciled_statement_lines_count"/>
                                                                <span class="o_stat_text">Transaction</span>
                                                            </div>
                                                        </button>
                                                        <button name="button_open_journal_entry"
                                                                type="object"
                                                                class="oe_stat_button"
                                                                icon="fa-bars"
                                                                invisible="not move_id"
                                                                groups="account.group_account_user,account.group_account_readonly">
                                                            <div class="o_stat_info">
                                                                <span class="o_stat_text">Journal Entry</span>
                                                            </div>
                                                        </button>
                                                    </div>

                                                    <widget name="web_ribbon" text="Invoicing App Legacy"
                                                            bg_color="text-bg-info"
                                                            invisible="state != 'invoicing_legacy'"
                                                            tooltip="This payment has been generated through the Invoicing app, before installing Accounting. It has been disabled by the 'Invoicing Switch Threshold Date' setting so that it does not impact your accounting."/>

                                                    <div class="oe_title">
                                                        <h1 invisible="state != 'draft'"><span>Draft</span></h1>
                                                        <h1 invisible="state == 'draft'"><field name="name" readonly="1"/></h1>
                                                    </div>

                                                    <group name="main_group">
                                                        <group name="group1">
                                                            <field name="payment_type" widget="radio" options="{'horizontal': True}"
                                                                   readonly="state != 'draft'"/>
                                                            <field name="partner_id" context="{'default_is_company': True}" string="Customer"
                                                                   options="{'no_quick_create': True}"
                                                                   invisible="partner_type != 'customer'"
                                                                   readonly="state != 'draft'"/>
                                                            <field name="partner_id" context="{'default_is_company': True}" string="Vendor"
                                                                   options="{'no_quick_create': True}"
                                                                   invisible="partner_type != 'supplier'"
                                                                   readonly="state != 'draft'"/>
                                                            <label for="amount"/>
                                                            <div name="amount_div" class="o_row">
                                                                <field name="amount"
                                                                       readonly="state != 'draft'"/>
                                                                <field name="currency_id"
                                                                       options="{'no_create': True, 'no_open': True}"
                                                                       required="1"
                                                                       readonly="state != 'draft'"
                                                                       groups="base.group_multi_currency"/>
                                                            </div>
                                                            <field name="company_currency_id" invisible="1"/>
                                                            <field name="other_currency" invisible="1"/>
                                                            <field name="force_amount_company_currency" invisible="1"/>
                                                            <label for="amount_company_currency" string="(on company currency)" invisible="not other_currency"/>
                                                            <div name="amount_company_currency" class="o_row" invisible="not other_currency">
                                                                x <field name="exchange_rate"/> = <field name="amount_company_currency" invisible="state != 'draft'"/>
                                                            </div>
                                                            <field name="date"
                                                                   readonly="state != 'draft'"/>
                                                            <field name="memo" string="Memo"/>
                                                        </group>
                                                        <group name="group2">
                                                            <field name="journal_id"
                                                                   domain="[('id', 'in', available_journal_ids)]"
                                                                   readonly="state != 'draft'"/>
                                                            <field name="payment_method_line_id"
                                                                   context="{'hide_payment_journal_id': 1}"
                                                                   options="{'no_create': True, 'no_open': True}"
                                                                   required="1"
                                                                   readonly="state != 'draft'"/>
                                                            <field name="partner_bank_id" string="Customer Bank Account"
                                                                   context="{'default_partner_id': partner_id, 'display_account_trust': True}"
                                                                   invisible="not show_partner_bank_account or partner_type != 'customer' or payment_type == 'inbound'"
                                                                   required="require_partner_bank_account"/>
                                                            <field name="partner_bank_id" string="Vendor Bank Account"
                                                                   context="{'default_partner_id': partner_id, 'display_account_trust': True}"
                                                                   invisible="not show_partner_bank_account or partner_type != 'supplier' or payment_type == 'inbound'"
                                                                   required="require_partner_bank_account"/>
                                                            <field name="partner_bank_id" string="Company Bank Account"
                                                                   context="{'default_partner_id': partner_id, 'display_account_trust': True}"
                                                                   invisible="not show_partner_bank_account or payment_type == 'outbound'"
                                                                   required="require_partner_bank_account"/>
                                                        </group>
                                                        <group>
                                                            <field name="qr_code" invisible="1"/>
                                                            <div invisible="not qr_code" colspan="2" class="text-center">
                                                                <field name="qr_code" widget="html"/>
                                                            </div>
                                                        </group>
                                                    </group>
                                                    <group>
                                                        <notebook name="payment_notebook">
                                                            <page name="Pagado" string="Pagado" invisible="state != 'paid'">
                                                                <group name="amounts">
                                                                    <group>
                                                                        <field name="matched_amount"/>
                                                                    </group>
                                                                    <group>
                                                                        <field name="unmatched_amount"/>
                                                                    </group>
                                                                </group>
                                                                <field name="matched_move_line_ids"></field>                                                              
                                                            </page>
                                                            <page name="Deudas" string="Deudas" invisible="state == 'paid'">
                                                                <field name="to_pay_move_line_ids" readonly="1">
                                                                </field>
                                                            </page>
                                                            <page name="Cheques" string="Cheques" invisible="payment_method_code not in ['in_third_party_checks', 'out_third_party_checks', 'return_third_party_checks', 'new_third_party_checks', 'own_checks']">
                                                                 <field name="l10n_latam_new_check_ids" readonly="0">
                                                                    <list name="new_checks" editable="bottom">
                                                                        <field name="company_id" column_invisible="True"/>
                                                                        <field name="currency_id" column_invisible="True"/>
                                                                        <field name="name"/>
                                                                        <field name="bank_id" column_invisible="parent.payment_method_code == 'own_checks'"/>
                                                                        <field name="issuer_vat" column_invisible="parent.payment_method_code == 'own_checks'"/>
                                                                        <field name="payment_date"/>
                                                                        <field name="amount"/>
                                                                        <button type="object" name="get_formview_action" icon="fa-pencil-square-o" title="open" help="Open" column_invisible="parent.state == 'draft'"/>
                                                                    </list>
                                                                </field>
                                                            </page>
                                                            <page name="Retenciones" string="Retenciones">
                                                                <field name="l10n_ar_withholding_line_ids">
                                                                </field>
                                                            </page>
                                                        </notebook>
                                                    </group>
                                                </sheet>
                                                <div class="o_attachment_preview"/>
                                                <chatter/>
                                            </form>
                                    </field>

                        </page>
                        <page string="Deudas" 
                              invisible="pop_up == True or payment_subtype != 'simple' or state != 'draft'">
                            <p>
                                El pago se comparará automáticamente con las líneas más antiguas de esta lista (por fecha de vencimiento). Puede eliminar cualquier línea que no desee que coincida.
                            </p>
                            <button name="add_all" 
                                    string="Agregar Todos / Refrescar" 
                                    invisible="state != 'draft'"
                                    type="object"/>
                            <button name="remove_all" 
                                    string="Remover Todos" 
                                    invisible="state != 'draft'"
                                    type="object"/>
                                    
                            <field name="debt_move_line_ids" 
                                   domain="[('partner_id.commercial_partner_id', '=', commercial_partner_id),('account_id.account_type', '=', account_internal_type),('account_id.reconcile', '=', True),('reconciled', '=', False), ('full_reconcile_id', '=', False),('company_id', '=', company_id)]" 
                                   groups="base.group_multi_company">
                            </field>
                        </page>
                        <page string="Pagado" 
                              invisible="state != 'posted'">
                            <field name="matched_move_line_ids"/>
                        </page>
                        <page string="Notas">
                            <field name="notes"
                            readonly="state in ['posted', 'confirmed', 'cancel']"/>
                        </page>
                    </notebook>
                                        
            </sheet>
            <div class="o_attachment_preview"/>
            <chatter/>
        </form>
    </field>
</record>

<record id="view_account_payment_group_graph" model="ir.ui.view">
    <field name="name">account.payment.group.graph</field>
    <field name="model">account.payment.group</field>
    <field name="arch" type="xml">
        <graph string="Pagos" type="bar" stacked="1">
            <field name="partner_id"/>
            <field name="payments_amount" type="measure"/>
        </graph>
    </field>
</record>

<record id="action_account_payments_group" model="ir.actions.act_window">
    <field name="name">Recibos de Cliente</field>
    <field name="res_model">account.payment.group</field>
    <field name="view_mode">list,form,graph</field>
    <field name="context">{'default_partner_type': 'customer', 'search_default_not_cancelled': 1}</field>
    <field name="domain">[('partner_type', '=', 'customer')]</field>
    <field name="help" type="html">
        <p class="oe_view_nocontent_create">
          Click para registrar un pago
        </p>
        <p>
          Los pagos se utilizan para registrar movimientos de liquidez (enviar o cobrar).
          A continuación, puede procesar esos pagos por sus propios medios o utilizando las medios instalados.
        </p>
    </field>
</record>

<record id="action_account_payments_group_payable" model="ir.actions.act_window">
    <field name="name">Pagos de Proveedor</field>
    <field name="res_model">account.payment.group</field>
    <field name="view_mode">list,form,graph</field>
    <field name="context">{'default_partner_type': 'supplier', 'search_default_not_cancelled': 1}</field>
    <!-- <field name="context">{'default_payment_type': 'outbound', 'default_partner_type': 'supplier'}</field> -->
    <!-- <field name="context">{'default_payment_type': 'outbound'}</field> -->
    <field name="domain">[('partner_type', '=', 'supplier')]</field>
    <!-- <field name="view_id" ref="view_account_supplier_payment_group_list"/> -->
    <field name="help" type="html">
        <p class="oe_view_nocontent_create">
          Click para registrar un pago
        </p>
        <p>
          Los pagos se utilizan para registrar movimientos de liquidez (enviar o cobrar).
          A continuación, puede procesar esos pagos por sus propios medios o utilizando las medios instalados.
        </p>
    </field>
</record>

<menuitem action="action_account_payments_group" id="menu_action_account_payments_group_receivable" parent="account.menu_finance_receivables" sequence="20"/>
<menuitem action="action_account_payments_group_payable" id="menu_action_account_payments_group_payable" parent="account.menu_finance_payables" sequence="20"/>

</data>
</odoo>
